import 'package:flutter/material.dart';


class CompletedApplication extends StatelessWidget {
  const CompletedApplication({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
